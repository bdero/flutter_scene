name: flutter_scene_importer
description: "An offline 3D model importer for Flutter."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.4.0-268.0.dev <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  #flat_buffers: ^23.5.26
  # TODO(bdero): Update after https://github.com/google/flatbuffers/pull/8289 is merged.
  args: ^2.5.0
  flat_buffers:
    git:
      url: https://github.com/NotTsunami/flatbuffers
      ref: 5665d6f5a92bd655cb0d5c95b4aeb32e527ff4af
      path: dart
  flutter:
    sdk: flutter
  flutter_gpu:
    sdk: flutter
  logging: ^1.2.0
  native_assets_cli: ^0.7.2
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
