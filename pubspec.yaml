name: flutter_scene
description: 3D scene widgets for Flutter.
version: 0.0.1-dev.1
repository: https://github.com/bdero/flutter_scene

environment:
  sdk: '>=3.0.0-122.0.dev <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  #flat_buffers: ^23.5.26
  # TODO(bdero): Update after https://github.com/google/flatbuffers/pull/8289 is merged.
  flat_buffers:
    git:
      url: https://github.com/NotTsunami/flatbuffers
      ref: 5665d6f5a92bd655cb0d5c95b4aeb32e527ff4af
      path: dart
  flutter:
    sdk: flutter
  flutter_gpu:
    sdk: flutter
  flutter_gpu_shaders: ^0.1.1
  #flutter_gpu_shaders:
  #  path: ../flutter_gpu_shaders
  flutter_scene_importer:
    path: ./importer
  native_assets_cli: ^0.7.2
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - build/shaderbundles/base.shaderbundle
    - assets/ibl_brdf_lut.png
