/// The component schema descriptor model lives in `package:scene/schema.dart`
/// (pure data, so schemas travel across process boundaries); re-exported here
/// for the engine-side consumers that grew up around this path.
library;

export 'package:scene/schema.dart';
