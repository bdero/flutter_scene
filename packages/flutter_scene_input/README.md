# flutter_scene_input

Rebindable input actions for Flutter Scene games.

Games read typed actions instead of keys. Action sets bind each action to physical controls through named slots, a context stack decides which sets are live, and read windows give frame and fixed-step code consistent values and edges.

This package is under active development and not yet published.
