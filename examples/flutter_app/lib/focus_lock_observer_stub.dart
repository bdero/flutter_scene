import 'package:flutter/foundation.dart';

FocusLockObserver createFocusLockObserver(VoidCallback onUnlocked) =>
    const FocusLockObserver();

class FocusLockObserver {
  const FocusLockObserver();

  void dispose() {}
}
