// Widget tests for the editor example require a GPU (SceneView). They are
// intentionally left empty here; visual verification is done by launching
// the app with flutter run.
//
// Headless descriptor and session tests live in
// packages/flutter_scene_editor/test/.
//
// TODO(headless-tests): add GPU-free smoke tests here when a mock scene
// fixture that doesn't require realization is available.
void main() {}
