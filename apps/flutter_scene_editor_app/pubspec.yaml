name: flutter_scene_editor_app
description: The standalone Flutter Scene Editor desktop application.
publish_to: none
version: 0.1.0

resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: '>=3.29.0-1.0.pre.242'

dependencies:
  flutter:
    sdk: flutter
  file_selector: ^1.0.0
  flutter_scene_editor:
    path: ../../packages/flutter_scene_editor
  flutter_scene_mcp:
    path: ../../packages/flutter_scene_mcp

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
